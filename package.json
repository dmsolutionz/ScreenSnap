{
  "name": "screensnap",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Free, local, watermark-free screenshot + screen recording Chrome extension (MV3). Records native MP4 entirely on-device, with a multi-track timeline video editor (trim, cut, zoom, audio, overlays).",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "make:icons": "node scripts/make-icons.mjs",
    "package": "node scripts/package.mjs",
    "lint": "eslint src",
    "preview": "node scripts/serve-docs.mjs",
    "smoke": "node scripts/smoke-extension.mjs",
    "smoke:share": "node scripts/smoke-share-player.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "10.6.0",
    "globals": "17.7.0"
  }
}
